<h1><%= @user.name %></h1>

<ul>
<% @user.recipes.each do |r| %>
<li> <%= r.title %> </li>
<% end %>
</ul>

<%= form_tag user_path(@user.id), method: "delete" do %>
<%= submit_tag "Delete #{@user.name}" %>
<% end %>
